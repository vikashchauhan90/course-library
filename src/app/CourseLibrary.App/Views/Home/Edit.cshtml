@model CourseFormViewModel

<section class="page-heading">
    <p class="eyebrow">Edit course</p>
    <h1>Make it sharper.</h1>
    <p class="lede">Only the course owner can update this course.</p>
</section>

<partial name="_CourseForm" model="Model" />
