@model bool
<div class="auth-shell"><div class="card auth-card"><div class="card-body text-center"><h1 class="h2">Email confirmation</h1><p class="text-secondary">@(Model ? "Your email address is confirmed." : "This confirmation link is invalid or expired.")</p><a class="btn btn-primary" asp-action="Login">Sign in</a></div></div></div>
